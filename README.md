# Booking system

Simple React UI for booking a room. Data is mock for now (nothing is saved on a server).

## Run

```bash
cd frontend
npm install
npm run dev
```

Open http://localhost:5173

- Pick your timezone, a room, and a date
- Open slots are clickable; taken slots show an error
- Bookings stay in the browser until you refresh
